# Frontend (Vite + React) - minimal scaffold

This is a minimal frontend scaffold for the MVP. It lives in /web and is configured for Vercel.

Run locally:
  cd web
  npm install
  npm run dev

Build for production:
  npm run build

